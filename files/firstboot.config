# firstboot.config
# Install this in /boot to setup networking on first boot

# This should be a FQDN
TARGET_HOSTNAME=TARGET_HOSTNAME

# Static IP of this machine's wired ethernet
HOST_IP=HOST_IP

# Gateway and DNS
NET_GATEWAY=NET_GATEWAY
NET_DNS=NET_DNS

